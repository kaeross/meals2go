import { colors } from "./colors";
import { Spacing, space, lineHeights } from "./spacing";
import { sizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./fonts";

export const theme = {
  colors,
  space,
  Spacing,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};
